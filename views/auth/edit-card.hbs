<br /><br />

<div class="loginPage">
  <h1
    style="padding-top:1.1rem; padding-bottom:0.5rem; padding-LEFT:0rem; text-align:center"
  >TRACE YOUR ROAD</h1>
  <br />
  <div class="card">
    <div class="container">
      <div id="card-form">
        <h3>Edit Card</h3>
        <hr class="cardHr" />
        <form action="/auth/edit-card/{{cardId}}" method="post">
          <label style="margin: 1rem">NOTES:<br />
            <input
            class="inputEdit"
              type="notes"
              name="notes"
              placeholder="Write here some notes about your trip"
              value="{{thisCard.notes}}"
            />
          </label style="margin: 1rem">
          <hr class="cardHr" />
          <label style="margin: 1rem">HIGHLIGHT:<br />
            <input
              class="inputEdit"
              type="favorites"
              name="favorites"
              placeholder="Write here the best of the trip"
              value="{{thisCard.favorites}}"
            />
          </label style="margin: 1rem">
          <hr class="cardHr" />
          <label style="margin: 1rem">CITIES I HAVE BEEN:<br />
            <input
            value="{{thisCard.cities}}"
              class="inputEdit"
              type="cities"
              name="cities"
              placeholder="Visited cities"
            />
          </label style="margin: 1rem">
          <hr class="cardHr" />
          <label style="margin-left:1rem; margin-top:1rem; margin-bottom:1rem; margin-right: 0.2rem">ARRIVAL DATE:
            <input style="color: rgb(89, 89, 89); width:10.5rem" type="date" name="arrivalDate" placeholder="Arrival Date" value="{{arrivDate.value}}"/>
          </label>
          <hr class="cardHr" />
          <label style="margin-left:1rem; margin-top:1rem; margin-bottom:1rem; margin-right: 0.2rem">DEPARTURE DATE:
            <input
            style="color: rgb(89, 89, 89);"
              type="date"
              name="departureDate"
              placeholder="Departure Date"
              value="{{departDate.value}}"
            />
          </label>
          <hr class="cardHr" />
          <br>
          <div style="align-items: center; display: flex; justify-content: center">
          <button class="textButtonUpdate" type="submit">UPDATE</button>
          </div>
        </form>
      </div>
    </div>
  </div>
  <br />
  <div class="button">
    <form class="buttonEdit" action="/auth/profile">
      <button class="textButtonEdit" type="submit">BACK TO PROFILE</button>
    </form>
  </div>
</div>